name: random_forest
description: Random forest classifier using the shared raw-data preprocessing pipeline.
model:
  type: random_forest_classifier
  n_estimators: 200
  max_depth: null
  min_samples_leaf: 10
  max_features: sqrt
  class_weight: balanced
  n_jobs: -1
  random_state: 42
